[workspace]
resolver = "3"
members = ["crates/*"]

[workspace.package]
version = "0.1.0"
rust-version = "1.89"
edition = "2024"
license = "MIT"
publish = true
authors = ["Nvisy <support@nvisy.com>"]
repository = "https://github.com/nvisycom/sdk-rs"
homepage = "https://nvisy.com"

[workspace.dependencies]
# HTTP
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart"] }
url = { version = "2.5", features = [] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = [] }

# Primitives
jiff = { version = "0.2", features = ["serde"] }
uuid = { version = "1.21", features = ["serde", "v4"] }

# Error handling
thiserror = { version = "2.0", features = [] }

# Derive macros
derive_builder = { version = "0.20", features = [] }

# Observability
tracing = { version = "0.1", features = [] }

# Dev
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }
